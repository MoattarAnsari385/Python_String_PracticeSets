# Write a program to reverse a given string. For example, if the input is "hello", the output should be "olleh".

str = "hello"
reversed_string = str[::-1] # use for reversed a string
print(reversed_string)
